Pod::Spec.new do |s|
  s.name     = "DialogSDK-GRPC"
  s.version  = "1.8.13"
  s.license  = "Apache License, Version 2.0"
  s.authors  = { 'Dialog Developers' => 'dev@dlg.im' }
  s.homepage = "https://github.com/dialogs/api-schema"
  s.summary = "DialogSDK-GRPC"
  s.source = { :git => 'https://github.com/dialogs/api-schema.git' }

  s.ios.deployment_target = "10.0"

  # Base directory where the .proto files are.
  #src = "../sdk-api/api-grpc/src/main/proto"
  src = "proto"

  # Run protoc with the Objective-C and gRPC plugins to generate protocol messages and gRPC clients.
  s.dependency "SwiftGRPC"
  s.dependency 'SwiftProtobuf'

  # Pods directory corresponding to this app's Podfile, relative to the location of this podspec.
  dependencies = 'swift_dependency'

  # Path where Cocoapods downloads protoc and the gRPC plugin.
  api_import = "include"
  protoc_dir = "#{dependencies}/!ProtoCompiler"
  protoc = "#{protoc_dir}/protoc"
  plugin = "#{dependencies}/GRPC/protoc-gen-swiftgrpc"
  descriptor = "#{api_import}/google/protobuf/descriptor.proto"

  # Directory where the generated files will be placed.
  dir = "#{s.name}"

  s.prepare_command = <<-CMD
    mkdir -p #{dir}

    #{protoc} \
        --plugin=#{plugin} \
        --plugin=#{dependencies}/Proto/protoc-gen-swift \
        --swift_out=Visibility=Public:#{dir}\
        --swiftgrpc_out=Client=true,Server=false,TestStubs=true,Visibility=Public:#{dir} \
        -I #{api_import} \
        -I #{src} \
        -I #{protoc_dir} \
        #{src}/*.proto  #{descriptor}
    CMD

    # Files generated by protoc
    s.subspec "Messages" do |ms|
    ms.source_files = "#{dir}/*.pb.{h,m,swift}", "#{dir}/**/*.pb.{h,m,swift}"
    ms.header_mappings_dir = dir
    ms.requires_arc = false
    # The generated files depend on the protobuf runtime.
    ms.dependency "Protobuf"
    ms.dependency 'SwiftProtobuf', '1.7.0'
    end

    # Files generated by the gRPC plugin
    s.subspec "Services" do |ss|
    ss.source_files = "#{dir}/*.grpc.{h,m,swift}", "#{dir}/**/*.pb.{h,m,swift}"
    ss.header_mappings_dir = dir
    ss.requires_arc = true
    # The generated files depend on the gRPC runtime, and on the files generated by protoc.
    ss.dependency "gRPC-ProtoRPC"
    ss.dependency "#{s.name}/Messages"
    end

  s.pod_target_xcconfig = {
    # This is needed by all pods that depend on Protobuf:
    'GCC_PREPROCESSOR_DEFINITIONS' => '$(inherited) GPB_USE_PROTOBUF_FRAMEWORK_IMPORTS=1',
    # This is needed by all pods that depend on gRPC-RxLibrary:
    'CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES' => 'YES',
  }
end
